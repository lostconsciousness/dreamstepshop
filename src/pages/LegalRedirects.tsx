import { Navigate } from 'react-router-dom';

export const PrivacyPolicyRedirect = () => <Navigate to="/privacy-policy" replace />;

export const TermsOfServiceRedirect = () => <Navigate to="/terms-of-service" replace />;
